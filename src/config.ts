export default {
    // testing guild id
    testingGuildId: '',

    // credentials
    bots: {
        production: {
            token: '',
            userId: '',
        },
    } as { [key: string]: { token: string; userId: string } },
};
